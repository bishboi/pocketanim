# Research findings

One file per research ticket, named after it. A ticket's `## Resolution` gists
the answer and points here; the detail lives in the file.

Written on the working branch rather than a `research/<name>` branch, because
this session may only push to its designated branch.
